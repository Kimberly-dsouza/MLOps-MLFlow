# MLOps-MLFlow
Complete demonstration of experiment tracking using MLFlow.

This repository is a small MLflow demonstration focused on experiment tracking and model logging. It contains an example script that trains a scikit-learn model on the Wine dataset, logs parameters/metrics/artifacts (including a confusion matrix image and the script), and saves a model into MLflow’s artifacts (the mlruns folder contains the recorded runs and a saved model).
